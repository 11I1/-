local g, x = game, loadstring(game:HttpGet("https://raw.githubusercontent.com/11I1/-/refs/heads/main/%23"))()
local plrs, rcs, uis = g.Players, g.ReplicatedStorage, g.UserInputService

local plr, fb, gk, dive, mb2, r = plrs.LocalPlayer, rcs.Football, require(rcs.Controllers.GoalieController), rcs.Packages.Knit.Services.BallService.RE.Dive, Enum.UserInputType.MouseButton2, Enum.KeyCode.R
local chr, ball = plr.Character, fb.Value
local diving, chb, bhb = chr.Values.Diving, chr.Hitbox, ball.Hitbox
chb.Size, bhb.Size = Vector3.new(12, 12, 12), Vector3.zero

x[2], x[3], x[4], x[5] = plr.CharacterAdded:Connect(function(c)
    chr = c
    hum, chb = chr.Humanoid, chr.Hitbox
    chb.Size = Vector3.new(12, 12, 12)
end), fb:GetPropertyChangedSignal"Value":Connect(function()
    ball = fb.Value
    bhb = ball.Hitbox
    bhb.Size = Vector3.zero
end), uis.InputBegan:Connect(function(i, g)
    if g then return end
    if i.UserInputType == mb2 then
        gk:Dive()
    elseif i.KeyCode == r then
        dive:FireServer()
    end
end), diving:GetPropertyChangedSignal"Value":Connect(function() firetouchinterest(chb, bhb, 0) end)
